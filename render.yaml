services:
  - type: web
    name: fmu-backend
    runtime: docker  # This is the critical line
    dockerfile: Dockerfile  # Explicitly point to your Dockerfile
    buildCommand: ""  # Clear any build commands
    startCommand: ""  # Clear any start commands
